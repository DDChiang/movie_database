<%= form_for(@spoiler) do |f| %>
  <% if @spoiler.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@spoiler.errors.count, "error") %> prohibited this spoiler from being saved:</h2>

      <ul>
      <% @spoiler.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <%= f.hidden_field :movie_id, value: session[:movie_id] %>  
  <%= f.hidden_field :user_id, value: current_user.id %>
  <div class="field">
    <%= f.label :synopsis %><br>
    <%= f.text_area :synopsis %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
