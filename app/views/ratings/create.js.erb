<% @rating = Rating.where('user_id' => params[:rating][:user_id], 'movie_id' => params[:rating][:movie_id]).first %>


$(function(){
  var html = "<%= escape_javascript(render @rating) %>";
  $("#movie_ratings").prepend(html);
  $("#create_rating").hide();
});
